#include <stdio.h>

/**
 * print_to_98 - Prints all natural numbers
 * from n to 98.
 *
 * @n: input number.
 *
 * Return: no return.
 */
void print_to_98(int x)
{
	if (x > 98)
	{
		for (; x > 98; n--)
		{
			printf("%d, ", x);
		}
	}
	else if (x < 98)
	{
		for (; x < 98; n++)
		{
			printf("%d, ", x);
		}
	}
	printf("%d\n", x);
}
